#config PLATFORM CPU
#config PROCESSES 2
#config CORES 4
#config MODE release

array<double,16,dist> a = {1.0};

double plusRand(double a){
	return a + mkt::rand(0,10);
}

main{
	a.show();
	a.mapInPlace(plusRand());
	a.show();
}
