#config PLATFORM CPU GPU
#config PROCESSES 4

#include test.h

const bool myConst = true;

int a[4] = {1 , 2, 3, 4};
double b[4];
bool c[4];
double globalVar = 123.2;

int f(int i, int j){
	double sum1 = 0;
 	double sum2 = 0;
	if(1 + 2){
		sum1 += 333;
		f(2);
	}
	a[0] = 42;
	sum1 += 333; 
	sum1 /= std::pow(sum1, 2);
	return i+1;
}

bool g(double p){
	return p > 2;
}

main{
	a.mapInPlace(f(1));
	b = a.map(f(42));
	
	if(2 < 5){
		c = a.map(g());
	}
}