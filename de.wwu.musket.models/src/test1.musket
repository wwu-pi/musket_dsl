#config PLATFORM CPU
#config PROCESSES 4

#include test.h

const bool myConst = true;

array<int,4,copy> a = {1 , 2, 3, 4};
array<double,4,copy> b;
array<bool,4,copy> c;
double globalVar = 123.2;

int f(int i , int j){
	double sum1 = 0;
 	double sum2 = 0;
	if(1 + 2){
		sum1 += 333;
		f(2);
	}
	for(int i : a ){
		a[j] = 42;
	}
	a.size();
	sum1 += 333; 
	sum1 /= std::pow(sum1, 2);
	return i+1;
}

bool g(double p, array<int,copy> b){
	for(int x : b){
		return false;
	}
	return p > 2;
}

main{
	a.mapInPlace(f(1, a[2]));
	for(int i = 0; i < 42; i++){
		b = a.map(f(42));
		b = a[[42 + 6]];
		b[2] = 5;
	}
	
	for(int i : a ){
		a[0] = 42;
	}
	
	if(2 < 5){
		c = a.map(g());
	}
}
