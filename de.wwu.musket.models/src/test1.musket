#config PLATFORM CPU GPU
#config PROCESSES 4

const bool myConst = true;

int a[5] = {1 , 2, 3, 4, 5};
double b[3];
double globalVar = 123.2;


int f(int i, int j){
	double sum1 = 0;
 	double sum2 = 0;
	sum1 += 333; 
	sum1 /= std::pow(sum1, 2);
	return i+1;
}

bool g(double p){
	return p*2;
}

main{
	for(a = 1; a < b; a++){
		a.mapInPlace(f(1));
		a = b.map(f(42));
	}
	
	if(a < 5 || b * 2){
		a.map(g(1.23));
		
		a = ( 9 + 3) * 6 + 4;
		a = a && (b || a + 2);
	}
}